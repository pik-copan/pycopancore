"""Config module for configuring pycopancore as an end user.

To override the following default configuration options,
import this as the first import in your study run script
and assign values as follows:
```
from pycopancore import config
config.profile = True  # turn on line profiling
```
"""

profile = False  # turn off line profiling 
